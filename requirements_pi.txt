# RichBot Pi 5 — Lightweight runtime dependencies
# Training/optimization must be done on desktop, then deploy to Pi

# Core trading — exchange.py uses direct HTTP (urllib), no ccxt needed on Pi
# ccxt>=4.0.0      # only needed on desktop or if WebSocket mode is enabled
# aiohttp>=3.9.0   # only needed on desktop

# Data
pandas>=2.0.0
numpy>=1.24.0
pandas-ta>=0.3.14b

# ML inference (optional — install manually if supported on your Python version)
# tflite-runtime>=2.14.0  # Not available for Python 3.13+; bot runs without ML
joblib>=1.3.0

# Database
asyncpg>=0.29.0
python-dotenv>=1.0.0

# Telegram + AI
python-telegram-bot>=20.0
openai>=1.12.0

# NOTE: These are intentionally excluded on Pi to save ~500MB RAM:
#   optuna — run optimization on desktop
#   streamlit, plotly — use Vercel dashboard instead
#   scikit-learn, scipy — only needed for training
#   SQLAlchemy — using raw sqlite3 + asyncpg instead
